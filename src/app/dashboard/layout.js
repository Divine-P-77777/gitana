export const metadata = {
    title: "Dashboard — Gitana Analysis",
    description: "View your repository score, improved roadmap, and analyze new projects with AI.",
};

export default function DashboardLayout({ children }) {
    return <>{children}</>;
}
